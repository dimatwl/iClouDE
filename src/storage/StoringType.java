package storage;

/**
 * Object of these types can be received by request to database.
 * @author Sergey
 *
 */
public enum StoringType {

	SOURCE_FILE_READER,
	SOURCE_FILE_WRITER,
	SOURCE_FILE,
	EXECUTABLE_FILE,
	USER
}
